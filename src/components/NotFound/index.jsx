import React from 'react'
import { NotFoundStyles } from './NotFound.styles'
// ===========================|| Loader ||=========================== //

const NotFound = () => {
	return (
		<NotFoundStyles>
			<h2 className='not-found'>Nota no encontrada.</h2>
		</NotFoundStyles>
	)
}

export default NotFound